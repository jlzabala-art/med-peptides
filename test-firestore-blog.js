const admin = require('firebase-admin');
const serviceAccount = require('./service-account.json'); // Maybe I can just use ADC, wait I can't in this sandbox.

console.log("Since I cannot run firebase-admin without credentials easily in the sandbox without ADC...");
