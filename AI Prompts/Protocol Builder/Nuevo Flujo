Refactor the Protocol Builder interface into a structured step-by-step clinical workflow optimized for laptop use.

Do not change clinical logic.
Do not remove existing modules.
Reorganize layout, navigation, and workflow sequence to create a clear linear experience.

The goal is to transform the current multi-section layout into a coherent step-driven clinical workflow similar to a structured “clinical cart” model.

==================================================
GLOBAL OBJECTIVE
==================================================

Create a sequential workflow with:

Step navigation
Persistent protocol workspace
Clear stage transitions
Minimal cognitive load

The workflow must follow:

STEP 1 — Patient Profile  
STEP 2 — Protocol Options  
STEP 3 — Safety Validation  
STEP 4 — Administration Plan  
STEP 5 — Final Review  
STEP 6 — Export & Save  

Each step must feel intentional and logically connected.

==================================================
SECTION 1 — ADD TOP WORKFLOW STEPPER
==================================================

Add a horizontal stepper at the top of the interface.

Steps:

1 Patient  
2 Protocol Options  
3 Safety  
4 Timeline  
5 Review  
6 Export  

Requirements:

Current step highlighted
Completed steps marked with check icon
Future steps inactive
Clickable navigation only to completed steps

Position:

Sticky top bar below main navigation.

==================================================
SECTION 2 — CREATE RIGHT-SIDE "PROTOCOL WORKSPACE"
==================================================

Add a persistent right sidebar that functions as a clinical workspace.

This replaces the concept of a cart.

Sidebar content:

Clinical Objective  
Selected Protocol  
Peptides Used  
Duration  
Tempo  
Safety Status  
Next Step Indicator  

Behavior:

Always visible on laptop
Updates dynamically as selections are made
Scrollable independently from main content

Purpose:

Maintain context across workflow.

==================================================
SECTION 3 — RESTRUCTURE STEP 1: PATIENT PROFILE
==================================================

Current Patient Profile section must be simplified and reorganized.

Display order:

1 Primary Clinical Objective  
2 Patient Demographics  
3 Clinical Preferences  
4 Practical Constraints  
5 Workflow Preference  

Remove unrelated protocol information from this step.

Button:

Replace:

"Generate Clinical Protocol"

With:

"Generate Protocol Options"

Output of this step:

Transition to Protocol Options stage.

==================================================
SECTION 4 — CREATE STEP 2: PROTOCOL OPTIONS
==================================================

Instead of generating a single protocol, generate:

Top 3 compatible protocol options.

Display as vertical ranked cards.

Each protocol card must include:

Priority rank (#1, #2, #3)

Protocol Name  
Peptides Used  
Duration  
Tempo  
Confidence Score  

Buttons:

[ Print Protocol ]  
[ Select Protocol ]

Sorting rule:

Highest confidence first.

Behavior:

Only one protocol can be selected.

Once selected:

Highlight selected card
Disable selection on others
Update sidebar workspace.

==================================================
SECTION 5 — RESTRUCTURE STEP 3: SAFETY VALIDATION
==================================================

Move Safety Intelligence Report into its own workflow step.

Display:

Safety Summary Header:

Confidence Score  
Validation Status  

Show validation cards:

Completeness  
Contraindications  
Dosing Logic  
Interactions  
Monitoring  

Each card must:

Have clickable icon
Open contextual popup explanation

Popup must include:

Validation description  
Detected issue (if exists)  
Clinical implication  
Recommended action  

Add:

[ Fix Issues ]  
[ Continue Anyway ]

buttons when warnings exist.

==================================================
SECTION 6 — RESTRUCTURE STEP 4: ADMINISTRATION PLAN
==================================================

Keep existing timeline structure but reorganize layout.

Left column:

Timeline Navigator

Right column:

Detailed Week Administration

Add:

Sticky protocol summary bar at top.

Display:

Protocol Name  
Peptides  
Duration  
Tempo  

Tabs remain:

Dosing  
Labs & Monitoring  
Safety Checks  
Economics  

==================================================
SECTION 7 — CREATE STEP 5: FINAL REVIEW
==================================================

Add new Final Review step.

Purpose:

Allow clinician confirmation before export.

Display:

Clinical Objective  
Selected Protocol  
Peptides Used  
Duration  
Tempo  
Safety Status  

Add:

Final Validation Badge:

Validated  
Review Required  

Primary Button:

[ Confirm Protocol ]

==================================================
SECTION 8 — CREATE STEP 6: EXPORT & SAVE
==================================================

Move all export and save functionality into final step.

Display:

Protocol Outputs:

[ Clinical Protocol PDF ]  
[ Patient Guide PDF ]  
[ Print Protocol ]  
[ Duplicate Protocol ]

Include:

Protocol Status Indicator:

Draft  
Validated  
Saved  

==================================================
SECTION 9 — REMOVE DUPLICATED MODULES
==================================================

Remove repeated sections appearing in multiple stages.

Specifically:

Repeated protocol title blocks  
Duplicated export buttons  
Repeated validation displays  

Each component must appear only once in its proper step.

==================================================
SECTION 10 — IMPROVE VISUAL FLOW CONSISTENCY
==================================================

Ensure:

Consistent spacing between sections
Consistent heading hierarchy
Clear separation between workflow steps

Avoid:

Large empty vertical gaps
Redundant containers
Visual duplication

==================================================
SECTION 11 — OPTIMIZE FOR LAPTOP WIDTH
==================================================

Target layout:

Main content width:

~1100–1200px

Sidebar width:

~320px

Ensure:

No excessive horizontal whitespace
Balanced visual density
Optimal readability

==================================================
SECTION 12 — TRANSITION ANIMATIONS
==================================================

Add subtle transitions between steps.

Use:

Fade transition  
Slide transition  

Avoid:

Abrupt layout jumps

==================================================
FINAL OBJECTIVE
==================================================

Transform the Protocol Builder into a structured, step-driven clinical workflow that mirrors real physician decision-making.

The new system must feel:

Sequential  
Predictable  
Clinical  
Reliable  

The workflow must guide users naturally from patient definition to final validated protocol export without confusion or duplication.