const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // assuming this exists or we can just run via node with GOOGLE_APPLICATION_CREDENTIALS if available.
