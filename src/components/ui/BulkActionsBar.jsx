import React from 'react';

/**
 * BulkActionsBar
 *
 * Obsolete: Replaced by Desktop Contextual Header above tables across all modules (Rule #3 & #7).
 */
export default function BulkActionsBar({ selectedCount, onClear, actions = [] }) {
  return null;
}
