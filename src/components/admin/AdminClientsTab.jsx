import React from 'react';

/**
 * Placeholder component for AdminClientsTab.
 * Replace with full implementation when available.
 */
const AdminClientsTab = ({ ownerId, ownerType }) => {
  return (
    <div style={{ padding: '2rem' }}>
      <h2>Clients Management</h2>
      <p>
        This is a placeholder for the AdminClientsTab component. It receives
        <code>ownerId</code> ({ownerId}) and <code>ownerType</code> ({ownerType})
        props.
      </p>
      {/* TODO: Implement client list, CRUD actions, etc. */}
    </div>
  );
};

export default AdminClientsTab;
