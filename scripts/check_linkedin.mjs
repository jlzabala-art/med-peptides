import { initializeApp } from 'firebase-admin/app'; import { getFirestore } from 'firebase-admin/firestore'; initializeApp({ projectId: 'med-peptides-app' }); const db = getFirestore(); db.collection('settings').doc('linkedin').get().then(doc => { console.log('Exists:', doc.exists); if(doc.exists) console.log(doc.data()); process.exit(0); }).catch(e => { console.error(e); process.exit(1); });
