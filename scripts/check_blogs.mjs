import { initializeApp } from 'firebase-admin/app'; import { getFirestore } from 'firebase-admin/firestore'; initializeApp({ projectId: 'med-peptides-app' }); const db = getFirestore(); async function check() { const c1 = await db.collection('blogs').get(); const c2 = await db.collection('posts').get(); const c3 = await db.collection('articles').get(); const c4 = await db.collection('blogPosts').get(); console.log('blogs:', c1.size, 'posts:', c2.size, 'articles:', c3.size, 'blogPosts:', c4.size); process.exit(0); } check();
